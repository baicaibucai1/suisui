{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "桌面端默认权限：窗口和事件 + 目录选择对话框。文件系统**没有开给前端** —— 仓库的读写一律走 main.rs 里那组 repo_* 命令（路径在里面被限制在仓库根内），不给前端一个能任意读写的 FS 通道。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
